# Advanced Actions patterns

- Reusable workflows (`workflow_call`) para centralizar lógica de CI e reduzir duplicação.
- Use `inputs` para parametrizar versões e comportamentos.
- Proteja secrets e use `environments` para deploys com approvers.
- Use artifact uploads para compartilhar resultados entre jobs ou workflows.
